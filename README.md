# **FFFFFlask DevOps Lab** 
## Usage
```
source .venv/bin/activate
pip install -r requirements.txt
python app.py

```
## APIs
+ /api/health: an endpoint that returns information about your app's health.
+ /api/config: an endpoint that returns information about your app's configuration.
+ /api/report: an endpoint that returns a status report.
